[package]
name = "frequenz-microgrid"
version = "0.5.1"
edition = "2024"
description = "A high-level interface to the Frequenz Microgrid API."
repository = "https://github.com/frequenz-floss/frequenz-microgrid-rs"
license = "MIT"

[lib]
name = "frequenz_microgrid"
path = "src/lib.rs"

[dependencies]
async-trait = "0.1.89"
chrono = "0.4"
frequenz-api-microgrid = { path = "../microgrid-api-rs", features = ["compile-well-known-types"] }
frequenz-microgrid-component-graph = "0.6.0"
frequenz-microgrid-formula-engine = "0.1.0"
frequenz-resampling = "0.2"
futures = "0.3.31"
tokio = { version = "1.48", features = ["rt", "rt-multi-thread"] }
tonic = "0.14"
tracing = { version = "0.1" }
tracing-subscriber = { version = "0.3" }
tokio-stream = { version = "0.1.17", features = ["sync"], optional = true }

[dev-dependencies]
tracing-subscriber = { version = "0.3", features = ["std", "env-filter"] }
tokio = { version = "1.48", features = ["test-util"] }
tokio-stream = { version = "0.1.17", features = ["sync"] }

[features]
test-utils = ["dep:tokio-stream"]
